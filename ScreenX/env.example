# ScreenX Environment Configuration
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit .env to git - it contains sensitive secrets

# Server Configuration
# Render provides PORT via environment. Default to 3000 locally.
PORT=3000

# Allowed origin for CORS and Socket.IO (set to your frontend URL in production)
ALLOWED_ORIGIN=

# MongoDB Configuration
MONGO_URI=mongodb://127.0.0.1:27017/screenx

# Session Secret (change to a random string) - REQUIRED in production
SESSION_SECRET=change-me-to-a-random-secret-string

# OpenAI API Key (optional - for AI features)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Redis Configuration
# IMPORTANT: Store REDIS_PASSWORD in environment variables or a secrets manager
# (e.g., GitHub Actions Secrets, AWS Secrets Manager, Docker secrets)
# Never commit the actual password to the repository.
REDIS_HOST=redis-11745.crce182.ap-south-1-1.ec2.cloud.redislabs.com
REDIS_PORT=11745
REDIS_PASSWORD=REPLACE_WITH_SECRET
REDIS_USERNAME=default

